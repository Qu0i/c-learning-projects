# 🧠 C Learning Projects

A collection of small C programs created during my learning journey.
Each mini-project focuses on a specific topic — from basic syntax to advanced concepts.


## 📂 Structure
```
.
├── pointers/         # Pointer practice
└── README.md
```

## 🧩 Example Projects
| Category   | Project           | Description                                         |
| ---------- | ----------------- | --------------------------------------------------- |
| pointers | `function_pointers.c`   | Simple calculator with input format `x y z`, where `y` is the operation (`+`, `-`, `*`, `/`), `x` and `y` are the operands.|
